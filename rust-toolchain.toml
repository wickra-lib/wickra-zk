# Host toolchain. The zkVM guest is built with its own toolchain, installed
# separately via `cargo risczero install` (see CONTRIBUTING.md and the
# setup-risc0 composite action); it is not governed by this file.
[toolchain]
channel = "1.88"
components = ["rustfmt", "clippy"]
