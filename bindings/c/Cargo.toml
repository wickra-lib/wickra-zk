[package]
name = "wickra-zk-c"
description = "C ABI (cdylib + staticlib) for wickra-zk — the hub every C-capable language (C, C++, C#, Go, Java, R) links against."
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lib]
name = "wickra_zk"
crate-type = ["cdylib", "staticlib", "rlib"]

# The C ABI inherently needs `unsafe` (raw pointers across the FFI boundary); the
# workspace forbids it everywhere else, so this crate re-allows it locally. The
# clippy set mirrors the workspace with the usual FFI-cast exemptions.
[lints.rust]
unsafe_code = "allow"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
missing_safety_doc = "allow"
cast_precision_loss = "allow"
cast_sign_loss = "allow"
cast_possible_truncation = "allow"
cast_possible_wrap = "allow"
similar_names = "allow"

[features]
default = ["prove"]
# The prover, forwarded to wickra-zk-host. Off, the C ABI verifies, commits and
# hashes only -- the `prove` command answers that this build verifies only --
# and builds for wasm32-unknown-emscripten, which is the R package's webR build
# on r-universe (bindings/r/configure passes --no-default-features there).
prove = ["wickra-zk-host/prove"]

[dependencies]
wickra-zk-host = { path = "../../crates/wickra-zk-host", version = "0.1.3", default-features = false }
